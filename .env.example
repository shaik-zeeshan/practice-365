# Postgres connection (matches docker-compose.yml local container)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/practice365

# Auth.js secret — generate with `openssl rand -base64 32`
AUTH_SECRET=replace-me
